# Cursor Rules — SMART Absen SMA UII (Monorepo Entrypoint)

## Project Context
- Stack: Laravel 13, InertiaJS 3 + React 19 + TypeScript 5.7, Tailwind CSS 4, Vite 8, PostgreSQL 16 (NeonDB)
- Auth: Laravel Sanctum + Spatie Laravel Permission
- Package Manager: Bun (frontend), Composer (backend)
- Architecture: Monorepo with submodules

## Code Style
- PHP: PSR-12 (Laravel Pint)
- React: Functional components + hooks, TypeScript strict, no `any` unless necessary
- CSS: Tailwind utility classes, no custom CSS unless necessary
- JS/TS: Bun for package management, no npm/yarn

## AI Behavior
- When writing code, always check existing patterns first
- Explain briefly before showing code (team members are learning)
- Security: validate all inputs, use prepared statements (Eloquent)
- Performance: avoid N+1 queries (use eager loading)
- Comments: only add if logic is non-obvious

## Conventions
- Routes: web.php for Inertia pages, api.php for Sanctum API
- Controllers: Inertia controller in `Controllers/Web/`, API controller in `Controllers/Api/`
- Business Logic: `app/Services/` — controller only calls service
- Validation: Form Request classes
- Database: migrations + seeders, PostgreSQL syntax
- Eloquent: eager loading, scopes for common queries

## Role Context
- This is the monorepo entrypoint. Most coding happens in core.git
- Use references: docs/, .opencode/, .openkb/, ../smauii-playbook/

## Commands
- bun run dev — Vite dev server (not npm!)
- bun run build — Production build
- ./vendor/bin/pint — Laravel Pint (code style fix)
- php artisan test — PHPUnit tests
